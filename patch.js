const fs = require('fs');
let code = fs.readFileSync('src/components/VariationColumn.tsx', 'utf8');

// The best way is to fetch the original code from github import or whatever... wait, I don't have git.
